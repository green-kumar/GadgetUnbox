package com.planb.test;

import java.util.ArrayList;
import java.util.Collections;

import com.planb.metadata.ProductMetaData;

public class DateComparatorTest {

	public static void main(String[] args) {
		
		/*ProductMetaData p1 = new ProductMetaData();
		p1.setReleaseDate("August 28, 2015 (Official)");
		ProductMetaData p2 = new ProductMetaData();
		p2.setReleaseDate("October 26, 2015 (Official)");
		ProductMetaData p3 = new ProductMetaData();
		p3.setReleaseDate("April 23, 2015 (Official)");
		
		ArrayList<ProductMetaData> l1 =new ArrayList<ProductMetaData>();
		l1.add(p1);
		l1.add(p2);
		l1.add(p3);
		
		Collections.sort(l1);
		for(int i=0;i<l1.size();i++){
		System.out.println(l1.get(i).getReleaseDate());
		}
		*/
		
		ArrayList<Object> a = null;
		
		for(Object o :a){
			
		}
		
		
		
		
	}
	
}
